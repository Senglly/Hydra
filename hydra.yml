dsn: ${DSN}

serve:
  public:
    port: 4444
    host: 0.0.0.0  # Important for Railway
  admin:
    port: 4445
    host: 0.0.0.0  # Important for Railway

urls:
  self:
    issuer: https://${RAILWAY_PUBLIC_URL}
  login: https://${RAILWAY_PUBLIC_URL}/login
  consent: https://${RAILWAY_PUBLIC_URL}/consent

secrets:
  system:
    - ${SECRETS_SYSTEM}

oauth2:
  expose_internal_errors: false

log:
  level: debug  # Change to 'info' in production