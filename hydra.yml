dsn: ${DSN}

serve:
  public:
    port: 4444
    host: 0.0.0.0
  admin:
    port: 4445
    host: 0.0.0.0

urls:
  self:
    issuer: https://${RAILWAY_PUBLIC_DOMAIN}
  login: https://${RAILWAY_PUBLIC_DOMAIN}
  consent: https://${RAILWAY_PUBLIC_DOMAIN}
secrets:
  system:
    - ${SECRETS_SYSTEM}

oauth2:
  expose_internal_errors: false

log:
  level: debug