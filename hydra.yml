# dsn: ${DSN}

# serve:
#   public:
#     port: 4444
#   admin:
#     port: 4445

# urls:
#   self:
#     issuer: http://localhost:4444
#   login: http://localhost:3000/login   # for now, can be a mock endpoint
#   consent: http://localhost:3000/consent

# secrets:
#   system:
#     - ${SECRETS_SYSTEM}

# oauth2:
#   expose_internal_errors: true


dsn: ${DSN}

serve:
  public:
    port: 4444
  admin:
    port: 4445

urls:
  self:
    issuer: https://${RAILWAY_PUBLIC_DOMAIN}
  login: https://${RAILWAY_PUBLIC_DOMAIN}/login
  consent: https://${RAILWAY_PUBLIC_DOMAIN}/consent



secrets:
  system:
    - ${SECRETS_SYSTEM}

oauth2:
  expose_internal_errors: false

