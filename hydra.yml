# dsn: ${DSN}  # REMOVE THIS LINE

serve:
  public:
    port: 4444
    host: 0.0.0.0
  admin:
    port: 4445
    host: 0.0.0.0

urls:
  self:
    issuer: https://${RAILWAY_STATIC_URL}
  login: https://${RAILWAY_STATIC_URL}/login
  consent: https://${RAILWAY_STATIC_URL}/consent

secrets:
  system:
    - ${SECRETS_SYSTEM}

oauth2:
  expose_internal_errors: false

log:
  level: debug